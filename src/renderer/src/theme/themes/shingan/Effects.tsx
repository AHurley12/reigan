import type { EffectsProps } from '../../types'

/** Stub for phase 1 — proves the lazy-swap mechanism. Ambient layer lands in a follow-up. */
export default function ShinganEffects(_props: EffectsProps) {
  return null
}
